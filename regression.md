# network-renderer-nebula Regression Notes

This file records current policy exceptions only. Keep entries exact and
current; do not use it as a session log.

## Architecture Shape

- policy=required | target=s88-style Enterprise/Site/Unit/EquipmentModule/ControlModule layout | reason=renderer code must stay in s88-style responsibility folders; top-level files are limited to flakes, tests, scripts/entrypoints, and thin imports into the renderer structure.
- policy=required | target=no oversized implementation files | reason=Nix implementation files over 200 LOC must be split by concrete renderer responsibility unless they are flake/test wiring or explicitly documented as a temporary regression with a split target.

## Live Lab Failures

- state=still-broken | target=site-local Nebula peer and unsafe-route return stability | evidence=2026-05-14 live branch-to-site-A DNS debugging showed the p2p/policy route chain was correct, but DNS replies from site-A mgmt DNS `10.20.10.1` intermittently failed to return over Nebula. Temporary runtime edits added peer `static_host_map` entries on `b-router-core-nebula` for `100.96.10.1`/`fd42:dead:beef:ee::1` and on `s-router-core-nebula` for `100.96.10.2`/`fd42:dead:beef:ee::2`; backups were kept as `/persist/etc/nebula/config.yml.pre-local-peer-map`. A `local_allow_list` experiment for the overlay CIDRs worsened the path and was reverted. Packet captures showed failed DNS replies existed on the site-A side but did not consistently arrive on the branch Nebula core. A separate live hotfix bypassed `nebula1` for the specific DNS lane by routing `10.20.10.1/32` and `10.60.10.0/24` over `overlay-west` with narrow temporary nft accepts, after which `dig -b 10.60.10.1 @10.20.10.1 example.com A` passed 30/30 attempts. | reason=the renderer/runtime must materialize stable local peer handling and preserve peer underlay/overlay routes without manual static-map edits, route overrides, or `temp-live-dns-*` nft bypasses; tests should prove Nebula restart does not make DNS-over-overlay intermittently lose return packets.
- state=solved | target=hostile IPv4 default-exit unsafe route propagation | evidence=2026-05-14 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-delegated-default-exit.sh` passed after the locked CPM update. The generated plan now gives `b-router-core-nebula` IPv4 split unsafe routes `0.0.0.0/1` and `128.0.0.0/1` plus the IPv6 split routes via the site-C overlay peer, and still forbids raw `0.0.0.0/0` and `::/0` unsafe defaults. | reason=the clean generated Nebula profile now covers the manual IPv4 split-default hotpatch without requiring `ip route replace` or disposable cert edits; endpoint public-egress success remains tracked separately until live validation is available.
- state=solved | target=delegated public egress unsafe routes | evidence=2026-05-14 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-delegated-default-exit.sh` passed and a direct generated-plan query found no public split defaults on `c-router-nebula-core`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-dynamic-delegated-return-route.sh` covers the site-C return route for the branch delegated prefix without making site-C a second public default source. | reason=delegated public egress split defaults are now installed on the branch Nebula entry side, while site-C carries only the dynamic return route needed for the delegated prefix.
- state=still-broken | target=delegated hostile IPv4 public egress | evidence=2026-05-13 s-router-test hostile-node01 ping to 1.1.1.1 failed while the endpoint default pointed to b-router-access-hostile; route ownership is still under investigation | reason=must be classified separately from the IPv6 Nebula loop before marking hostile public egress production-safe.
