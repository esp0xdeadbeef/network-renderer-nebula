# network-renderer-nebula regression state

- state=solved | target=public-forwarded-runtime-node-endpoint-collides-with-lighthouse-4242 | evidence=2026-05-15 fresh s-router lab validation showed the generated home profile static-mapped `hetz-router-nebula-core` to the same public `4242` tuple owned by the Hetz DMZ lighthouse service. The renderer bug was in `s88/Enterprise/bootstrap/nixos-module/profile-bootstrap.bash`: `externalSuppressPublicLighthouseStaticMap=1` blanked `public_lighthouse_endpoint`, then collision checks compared public-forwarded runtime nodes against that blank value. The fix keeps separate `lighthouse_owned_endpoint*` values and compares advertise/static-map endpoints against those original lighthouse-owned endpoints, hard-failing with `model a unique public endpoint or remove the public-forwarded node` when no distinct endpoint is modeled. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-bootstrap-module.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-public-forwarded-relay-static-map.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-regression-md-resolved-states.sh` pass. | reason=The Nebula renderer now materializes only explicit public-forward endpoints and refuses to advertise a non-lighthouse runtime node on the same public tuple as the lighthouse service; remaining live failures must come from upstream intent/CPM/runtime facts or lock propagation, not renderer-side static-map invention.
