# network-renderer-nebula Regression Notes

This file records current policy exceptions only. Keep entries exact and
current; do not use it as a session log.

## Architecture Shape

- state=required | target=s88-style Enterprise/Site/Unit/EquipmentModule/ControlModule layout | reason=renderer code must stay in s88-style responsibility folders; top-level files are limited to flakes, tests, scripts/entrypoints, and thin imports into the renderer structure.
- state=required | target=no oversized implementation files | reason=Nix implementation files over 200 LOC must be split by concrete renderer responsibility unless they are flake/test wiring or explicitly documented as a temporary regression with a split target.

## Live Lab Failures

- state=still-broken | target=site-local Nebula peer and unsafe-route return stability | evidence=2026-05-14 live branch-to-site-A DNS debugging showed the p2p/policy route chain was correct, but DNS replies from site-A mgmt DNS `10.20.10.1` intermittently failed to return over Nebula. Temporary runtime edits added peer `static_host_map` entries on `b-router-core-nebula` for `100.96.10.1`/`fd42:dead:beef:ee::1` and on `s-router-core-nebula` for `100.96.10.2`/`fd42:dead:beef:ee::2`; backups were kept as `/persist/etc/nebula/config.yml.pre-local-peer-map`. A `local_allow_list` experiment for the overlay CIDRs worsened the path and was reverted. Packet captures showed failed DNS replies existed on the site-A side but did not consistently arrive on the branch Nebula core. A separate live hotfix bypassed `nebula1` for the specific DNS lane by routing `10.20.10.1/32` and `10.60.10.0/24` over `overlay-west` with narrow temporary nft accepts, after which `dig -b 10.60.10.1 @10.20.10.1 example.com A` passed 30/30 attempts. | reason=the renderer/runtime must materialize stable local peer handling and preserve peer underlay/overlay routes without manual static-map edits, route overrides, or `temp-live-dns-*` nft bypasses; tests should prove Nebula restart does not make DNS-over-overlay intermittently lose return packets.
- state=still-broken | target=hostile IPv4 default-exit unsafe route propagation | evidence=2026-05-14 concrete live hotpatches: in `b-router-core-nebula`, manually added `ip route replace 0.0.0.0/1 dev nebula1 scope link mtu 1200` and `ip route replace 128.0.0.0/1 dev nebula1 scope link mtu 1200` because the generated profile only installed IPv6 split defaults `::/1` and `8000::/1`; in `c-router-nebula-core`, reissued a disposable cert through the external CA-unseal flow with `0.0.0.0/1` and `128.0.0.0/1` added to unsafeNetworks because generated cert inputs omitted them. After profile redistribution and Nebula restarts, `hostile-node01` endpoint IPv4 public TCP/HTTP still timed out, so these hotpatches are necessary regression evidence but not sufficient for production readiness. | reason=tests should assert the generated Nebula bootstrap/profile plan includes IPv4 split-default unsafe routes and cert unsafeNetworks for the modeled hostile-Hetzner egress path, and that the clean output survives Nebula restart without manual `ip route replace` or disposable cert signing.
- state=still-broken | target=delegated public egress unsafe routes | evidence=2026-05-13 s-router-test hostile-node01 IPv6 ping to 2606:4700:4700::1111 failed with hop-limit exceeded from fd42:dead:beef:ee::3; Hetzner c-router-nebula-core installed ::/1 and 8000::/1 on nebula1 and selected nebula1 for the public route from hostile delegated GUA | reason=the renderer materializes delegated-public-egress split defaults on the site-C exit node instead of only on the remote entry side, creating an overlay loop.
- state=still-broken | target=delegated hostile IPv4 public egress | evidence=2026-05-13 s-router-test hostile-node01 ping to 1.1.1.1 failed while the endpoint default pointed to b-router-access-hostile; route ownership is still under investigation | reason=must be classified separately from the IPv6 Nebula loop before marking hostile public egress production-safe.
