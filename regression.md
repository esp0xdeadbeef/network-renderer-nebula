# network-renderer-nebula Regression Notes

This file records current policy exceptions only. Keep entries exact and
current; do not use it as a session log.

## Architecture Shape

- policy=required | target=s88-style Enterprise/Site/Unit/EquipmentModule/ControlModule layout | reason=renderer code must stay in s88-style responsibility folders; top-level files are limited to flakes, tests, scripts/entrypoints, and thin imports into the renderer structure.
- policy=required | target=no oversized implementation files | reason=Nix implementation files over 200 LOC must be split by concrete renderer responsibility unless they are flake/test wiring or explicitly documented as a temporary regression with a split target.

## Live Lab Failures

- state=solved | target=FS-310-HDS-010-SDS-010-SMS-020 all-systems package evaluation | evidence=2026-07-19 focused test `tests/FS-310-HDS-010-SDS-010-SMS-020.sh` passes for x86_64-linux and aarch64-linux with import-from-derivation disabled, and `nix flake check --all-systems --no-update-lock-file` passes; package template substitution now uses pure string replacement instead of reading a `pkgs.replaceVars` derivation | reason=the flake explicitly advertises both supported systems, so each package output evaluates without host-architecture build execution.
- state=solved | target=FS-180 explicit return-authority dependency freshness | evidence=2026-07-19 the lock was refreshed to pushed CPM `406de6f4`, NFM `1ab37e18`, and direct network-labs `665bdc0e`; focused Nebula plan and CLI construction tests now accept the explicit `returnBehavior` chain | reason=this renderer's pinned tests now exercise current upstream authority instead of stale fixtures that cannot enter CPM.
- state=solved | target=renderer flake responsibility size | evidence=2026-07-19 package construction moved to `s88/Enterprise/package.nix`; `flake.nix` is 241 lines and `tests/test-nix-file-loc.sh` passes | reason=package construction is a separable Enterprise-level wiring responsibility behind a thin flake import.

- state=solved | target=default per-container full tcpdump capture service compatibility | evidence=2026-06-27 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-010.sh` passed after `s88-runtime-tcpdump.service` was materialized by the Nebula runtime module with `multi-user.target`, `RuntimeDirectory=s88-runtime-tcpdump`, full-snaplen `tcpdump -i any`, restart policy, and per-node pcap output under `/run/s88-runtime-tcpdump`. | reason=the renderer now emits the shared runtime-debugger service contract needed for post-failure HAT/SAT inspection; actual live capture contents remain HAT/SAT evidence.
- state=solved | target=site-local Nebula peer and unsafe-route return stability | evidence=2026-06-27 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-010.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-030.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-030.sh` passed. The plan test preserves site-local static unsafe routes, overlay host preparation, public underlay endpoints, and rejects raw defaults; the relay/static-map tests prove explicit relay listen hosts, public endpoint secret maps, and local-cidr firewall scoping without learned private underlay addresses. | reason=the renderer-owned static map, relay, underlay endpoint, and return-route materialization is now covered by focused SMT evidence; live restart and DNS packet continuity remain HAT/SAT evidence.
- state=solved | target=hostile IPv4 default-exit unsafe route propagation | evidence=2026-06-27 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-050.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-090.sh` passed. The generated plan gives branch-side IPv4 split unsafe routes `0.0.0.0/1` and `128.0.0.0/1`, IPv6 split routes, split-default firewall coverage, and remote advertised split defaults, while still forbidding raw `0.0.0.0/0` and `::/0`. | reason=the generated Nebula profile now covers the manual split-default hotpatch without requiring `ip route replace` or disposable cert edits; endpoint public-egress packet success remains HAT/SAT evidence.
- state=solved | target=delegated public egress unsafe routes | evidence=2026-06-27 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-050.sh` passed and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-050.sh` passed. The tests prove delegated public egress defaults are installed only on the branch Nebula entry side as split non-installing unsafe routes, while site-C carries dynamic return-route/firewall source-file authority without becoming a second public default source. | reason=delegated public egress route authority is now construction-proven in the renderer; live public-egress packet acceptance remains HAT/SAT evidence.
- state=solved | target=delegated hostile IPv4 public egress | evidence=2026-06-27 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-050.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-050.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/FS-460-HDS-010-SDS-010-SMS-090.sh` passed. The tests prove hostile delegated IPv4 public egress keeps source-file authority, emits branch-side split defaults, scopes public ingress runtime facts from CPM data, and advertises/firewalls only split defaults. | reason=the renderer-owned construction bug is now covered separately from the earlier live ping failure; HAT/SAT must still prove actual hostile-node public egress packets.
