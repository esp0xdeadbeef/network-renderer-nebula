# network-renderer-nebula regression state

- state=fixed-locally | target=delegated-prefix-secret-name-shortcut-leak | evidence=2026-05-21 full-loop rerun reused the existing Hetzner validator and failed before validation because `s-router-model-host` derived six access-node prefix secret names, including old `externalValidation.delegatedPrefixSecretName` shortcuts such as `espbranch-*` and `esp0xdeadbeef-*`, while the live/runtime routed-prefix secrets only exist for the modeled advertisement source files. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-delegated-prefix-secret-names.sh` now proves only `advertisements.ipv6Ra[*].delegatedPrefix.sourceFile` contributes routed-prefix secret names. | reason=The Nebula renderer helper must enumerate real delegated prefix source files from CPM/provider output. Treating `externalValidation` shortcuts as runtime secrets reintroduces stale validation-only inventory into the rebuild loop and makes orchestration expect prefixes that are not part of the model.

- state=solved | target=public-ingress-runtime-listen-host-source | evidence=2026-05-20 `buildNebulaPublicIngressRuntimeFacts` now derives the runtime forward target from `runtimeNode.service.listenHost` and derives the interface prefix length from the modeled host-NAT WAN CIDR; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-public-ingress-runtime-facts.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-bootstrap-module.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-plan.sh` pass. | reason=The private public-ingress runtime listener address is an inventory/provider realization fact already present in the Nebula runtime node. Hetzner/NixOS host glue may select and wire the node, but must not restate the listener address locally.

- state=solved | target=nebula-public-relay-endpoint-static-map | evidence=2026-05-19 `network-labs` now models the Hetzner Nebula core relay public runtime endpoint as `service.publicEndpoints = [{ endpointSourceFile = "/run/secrets/hetzner-public-ipv4"; port = 4243; }]`. The renderer resolves relay node identities after `relay-resolution.nix`, emits `staticHostMap["100.96.10.3"] = ["127.0.0.1:4243"]`, rewrites that placeholder from the SOPS-backed runtime secret in the NixOS preStart path, and coerces string or integer source ports to the integer expected by the NixOS Nebula module. Verified with `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-public-relay-endpoint-static-map.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-runtime-module.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh`. | reason=Remote Nebula relay reachability must come from explicit lab inventory/runtime facts and resolved CPM relay contracts. The renderer may materialize those fields, but must not learn a private underlay address from Nebula logs or infer public relay endpoints from names.

- state=solved | target=nebula-runtime-sops-and-service-materialization | evidence=2026-05-17 renderer rewrite removed `profile-bootstrap.bash` and `ca-unseal.bash`; bootstrap now declares SOPS-backed profile file targets under `/persist/nebula-runtime/profiles`, runtime nodes are precomputed from CPM/provider data, and runtime modules use NixOS `services.nebula.networks` with systemd `AssertPathExists` guards instead of custom `nebula -config` units and route-prep shell hooks. Focused validation passed: `bash tests/test-nebula-bootstrap-module.sh`, `bash tests/test-nebula-runtime-module.sh`, `bash tests/test-nebula-plan.sh`, `bash tests/test-nebula-remote-lighthouse-endpoint.sh`, `bash tests/test-nebula-public-forwarded-relay-static-map.sh`, `bash tests/test-nebula-dynamic-delegated-return-route.sh`, `bash tests/test-nebula-delegated-default-exit.sh`, provider-boundary spot checks for `s-router-overlay-dns-lane-policy`, `s-router-public-overlay-service`, `dual-wan-branch-overlay`, `overlay-east-west`, `single-wan-with-nebula`, and `single-wan-with-nebula-any-to-any-fw`, and `bash tests/test-regression-md-resolved-states.sh`. | reason=The Nebula renderer now materializes explicit Nebula provider output through standard NixOS Nebula service options and SOPS-provisioned certificates, while underlay reachability remains a source intent/CPM contract rather than renderer-side inference or shell glue.
- state=solved | target=dynamic-delegated-nebula-cert-unsafe-networks | evidence=2026-05-20 full loop reached live hostile IPv6 public-egress after Site-C DNS over Nebula passed. `nixos-router-core-nebula` runtime config included `2a01:4f9:c01f:2::/64` as a dynamic Nebula firewall `local_cidr`, but `nebula-cert print` for `nixos-router-core-nebula` did not include that delegated GUA prefix in `unsafeNetworks`; packets entered local `nebula1` and never appeared on remote `hetz-router-nebula-core` `nebula1`. The renderer now emits `advertisedUnsafeNetworkSourceFiles` from modeled dynamic delegated firewall CIDRs, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-bootstrap-spec.sh` plus `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nix-file-loc.sh` pass. | reason=Dynamic delegated public-egress prefixes are now part of the explicit Nebula bootstrap/profile contract as source-file references; runtime secret resolution happens in orchestration without hardcoding the delegated prefix in renderer output or lab topology glue.
- state=solved | target=dynamic-delegated-nebula-peer-return-routes | evidence=2026-05-20 after the public runtime ingress fix, the full loop reached Hetz Nebula handshake and Site-C DNS A/AAAA over Nebula, but hostile public IPv6 egress still failed. Live tcpdump showed hostile GUA echo requests entered `hetz-router-nebula-core` on `nebula1` and left `upstream`; Cloudflare replies entered `upstream`, then the Hetz core routed the delegated `2a01:4f9:c01f:2::/64` destination back out `upstream` table 2002 instead of `nebula1`. The renderer now derives peer `dynamicUnsafeRoutes` from explicit source-file delegated runtime CIDRs, materializes them into the Nebula runtime config before service start, and validates the s-router three-site Hetz route with `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-dynamic-delegated-return-route.sh`; full renderer suite passed with `NETWORK_REPO_DIRECT_TEST_OK=1 ./tests/test.sh`. | reason=Peer return routes for dynamic delegated public prefixes are explicit CPM/provider output. The Nebula renderer may resolve source files at runtime and write Nebula `tun.unsafe_routes`, but NixOS host glue must not invent overlay return paths.
