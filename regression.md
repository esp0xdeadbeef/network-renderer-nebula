# network-renderer-nebula regression state

- state=solved | target=standalone-cli-offline-flake-inputs | evidence=2026-05-24 live `nixos-router-core-nebula` reached the manual service preflight and had the needed runtime secrets mounted, but `network-renderer-nebula render-node` failed before writing `runtime-node.json` because its internal `nix eval` tried to download `github:NixOS/nixpkgs/...` and DNS to `github.com`/`cache.nixos.org` is intentionally unavailable under the leak-prevention policy. The packaged CLI now imports its own `s88/Enterprise/default.nix` plus the packaged nixpkgs `lib` store path directly, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-cli-render-node.sh` passes while asserting the runner no longer contains `builtins.getFlake`. | reason=The Nebula renderer owns the standalone CLI materialization contract. The local NixOS harness should not open public DNS or reinterpret Nebula runtime data just so the CLI can evaluate.

- state=solved | target=standalone-cli-readonly-nix-db | evidence=2026-05-24 live `nixos-router-core-nebula` manual service reached the locked `network-renderer-nebula` CLI but failed with `error: opening lock file "/nix/var/nix/db/big-lock": Read-only file system`; running the same CLI with `NIX_REMOTE=local?root=/tmp/nix-nebula-test` rendered `runtime-node.json` successfully from `/etc/network-artifacts/{control-plane,inventory}.json`. The CLI now falls back to a temporary local Nix store when `/nix/var/nix/db` is not writable, and `tests/test-cli-render-node.sh` exercises `render-node` with `NIX_REMOTE=local?root=...`. | reason=The Nebula renderer owns the standalone `render-node` CLI contract. Consumers may provide CPM/inventory artifacts and certs, but they should not need a writable host Nix database inside a disposable nspawn router just to materialize a Nebula node.

- state=solved | target=standalone-cli-runtime-secret-and-nix-config-contract | evidence=2026-05-23 focused renderer checks prove the standalone CLI and runtime module now cover the previously missing pieces without requiring NixOS or s-router-test to reinterpret Nebula semantics: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-cli-render-node.sh` builds the app wrapper and runs `render-node --cpm <bundle> --node b-router-core-nebula`, while `bin/network-renderer-nebula` calls internal `nix eval` with `--extra-experimental-features "nix-command flakes"`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-runtime-module.sh` proves the runtime NixOS module rewrites placeholder lighthouse/static-map endpoints from explicit `/run/secrets/hetzner-lighthouse-public-ipv4`, `/run/secrets/hetzner-public-ipv6`, and delegated-prefix source files during `preStart`, emits `/run/nebula-runtime/runtime.yml`, and keeps firewall/forwarding policy out of this renderer. The s-router-test lab regression remains the live gate for invoking `nix run github:esp0xdeadbeef/network-renderer-nebula -- render-node ...` inside the rebuilt core container with real CPM artifacts and profile files. | reason=The Nebula renderer owns Nebula node/config/service materialization from explicit CPM/provider data and runtime secret paths, including placeholder endpoint substitution. Underlay reachability, host firewall, TUN device exposure, and container capability plumbing remain consumer/renderer-nixos responsibilities.

- state=solved | target=standalone-cpm-node-render-cli | evidence=2026-05-22 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-cli-render-node.sh` exercises `nix run . -- render-node --cpm <bundle> --node <node>` against the s-router overlay CPM bundle shape, writes `runtime-node.json` for a modeled core node, and verifies an unmanaged laptop node requires explicit `--extra-node --overlay --addr4 --addr6` input while emitting no unsafe routes. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nixos-module-no-host-reachability-policy.sh` also passes. | reason=Nebula membership materialization belongs in the Nebula renderer and must be invocable without the NixOS renderer or `s-router-test` reparsing provider semantics. Unmodeled laptops may join only as explicit overlay members; topology forwarding remains upstream model/CPM data.

- state=solved | target=public-ingress-runtime-forward-port-source | evidence=2026-05-21 full rebuild-loop failed Site-C DNS-over-Nebula after `hetz-router-nebula-core` correctly listened on `172.31.254.4:443` and `nixos-router-core-nebula` static-mapped `100.96.10.3` to `135.181.207.194:443`, but Hetz host nft still DNATed `s88-public-runtime-forward-0` on UDP/4243. Live hotpatching only the public runtime DNAT/forward dport to 443 made Nebula peer pings work and made `nixos-router-access-hostile dig -b 10.20.70.1 @10.90.10.1 example.com A/AAAA` return `NOERROR`. `tests/test-public-ingress-runtime-facts.sh` now omits any caller-supplied runtime dport and proves the renderer derives runtime forward input dports from `runtimeNode.service.port`. | reason=The public runtime relay port is explicit Nebula provider output. NixOS host composition may choose the host/container wiring, but must not restate or hardcode the runtime forward port.

- state=solved | target=hetz-public-ingress-all-wan-services | evidence=2026-05-22 live `hetzner-nebula-prodtest-01` nft only contained `s88-public-service-dmz-nebula` and `s88-public-runtime-forward-0`; the CPM artifact also modeled WAN service relations for `nixos-hostile-4444`, `clab-client-4445`, and `hetz-client-4446`, but those service facts were absent from the Nebula public-ingress runtime facts consumed by `network-renderer-nixos`. `buildNebulaPublicIngressRuntimeFacts` now exports public ingress facts for every selected-site WAN service relation while preserving the separate runtime forward, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-public-ingress-runtime-facts.sh` passes. | reason=The Hetzner public edge is a Nebula/provider runtime fact supplier for the NixOS renderer. It must pass through all explicit WAN-to-service CPM relations for the selected site, not only the Nebula lighthouse service, otherwise the host nft can never materialize modeled public port forwards.

- state=solved | target=external-lighthouse-runtime-static-map-port | evidence=2026-05-21 full-loop reached Site-C DNS over Nebula and failed because `nixos-router-core-nebula` accepted the DNS packet out `nebula1`, but `hetz-router-nebula-core` never saw it. Live `nebula@runtime` logs on Hetz showed `listener is IPv4, but writing to IPv6 remote` and the rendered runtime config rewrote the external lighthouse static map to the relay runtime port, even though the external lighthouse listens on `4242`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-public-relay-endpoint-static-map.sh` now checks that dynamic external lighthouse replacement uses `runtimeNode.lighthouse.port` and suppresses unusable IPv6 endpoints for IPv4-only listeners. | reason=The node runtime listen port belongs to the relay/public-ingress service, not to the external lighthouse endpoint. The renderer must materialize the explicit lighthouse port from CPM/provider output and must not make Nebula try IPv6 endpoints from an IPv4-only listener.

- state=solved | target=delegated-prefix-secret-name-shortcut-leak | evidence=2026-05-21 full-loop rerun reused the existing Hetzner validator and failed before validation because `s-router-model-host` derived six access-node prefix secret names, including old `externalValidation.delegatedPrefixSecretName` shortcuts such as `espbranch-*` and `esp0xdeadbeef-*`, while the live/runtime routed-prefix secrets only exist for the modeled advertisement source files. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-delegated-prefix-secret-names.sh` now proves only `advertisements.ipv6Ra[*].delegatedPrefix.sourceFile` contributes routed-prefix secret names. | reason=The Nebula renderer helper must enumerate real delegated prefix source files from CPM/provider output. Treating `externalValidation` shortcuts as runtime secrets reintroduces stale validation-only inventory into the rebuild loop and makes orchestration expect prefixes that are not part of the model.

- state=solved | target=public-ingress-runtime-listen-host-source | evidence=2026-05-20 `buildNebulaPublicIngressRuntimeFacts` now derives the runtime forward target from `runtimeNode.service.listenHost` and derives the interface prefix length from the modeled host-NAT WAN CIDR; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-public-ingress-runtime-facts.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-bootstrap-module.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-plan.sh` pass. | reason=The private public-ingress runtime listener address is an inventory/provider realization fact already present in the Nebula runtime node. Hetzner/NixOS host glue may select and wire the node, but must not restate the listener address locally.

- state=solved | target=nebula-public-relay-endpoint-static-map | evidence=2026-05-21 `network-labs` now models the Hetzner Nebula core relay public runtime endpoint as `service.publicEndpoints = [{ endpointSourceFile = "/run/secrets/hetzner-public-ipv4"; port = 443; }]`, matching live evidence that the lab-underlay path reaches Hetz UDP/443 and not UDP/4243. The renderer resolves relay node identities after `relay-resolution.nix`, emits `staticHostMap["100.96.10.3"] = ["127.0.0.1:443"]`, rewrites that placeholder from the SOPS-backed runtime secret in the NixOS preStart path, and coerces string or integer source ports to the integer expected by the NixOS Nebula module. Verified with `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-public-relay-endpoint-static-map.sh`. | reason=Remote Nebula relay reachability must come from explicit lab inventory/runtime facts and resolved CPM relay contracts. The renderer may materialize those fields, but must not learn a private underlay address from Nebula logs or infer public relay endpoints from names.

- state=solved | target=nebula-runtime-sops-and-service-materialization | evidence=2026-05-17 renderer rewrite removed `profile-bootstrap.bash` and `ca-unseal.bash`; bootstrap now declares SOPS-backed profile file targets under `/persist/nebula-runtime/profiles`, runtime nodes are precomputed from CPM/provider data, and runtime modules use NixOS `services.nebula.networks` with systemd `AssertPathExists` guards instead of custom `nebula -config` units and route-prep shell hooks. Focused validation passed: `bash tests/test-nebula-bootstrap-module.sh`, `bash tests/test-nebula-runtime-module.sh`, `bash tests/test-nebula-plan.sh`, `bash tests/test-nebula-remote-lighthouse-endpoint.sh`, `bash tests/test-nebula-public-forwarded-relay-static-map.sh`, `bash tests/test-nebula-dynamic-delegated-return-route.sh`, `bash tests/test-nebula-delegated-default-exit.sh`, provider-boundary spot checks for `s-router-overlay-dns-lane-policy`, `s-router-public-overlay-service`, `dual-wan-branch-overlay`, `overlay-east-west`, `single-wan-with-nebula`, and `single-wan-with-nebula-any-to-any-fw`, and `bash tests/test-regression-md-resolved-states.sh`. | reason=The Nebula renderer now materializes explicit Nebula provider output through standard NixOS Nebula service options and SOPS-provisioned certificates, while underlay reachability remains a source intent/CPM contract rather than renderer-side inference or shell glue.
- state=solved | target=dynamic-delegated-nebula-cert-unsafe-networks | evidence=2026-05-20 full loop reached live hostile IPv6 public-egress after Site-C DNS over Nebula passed. `nixos-router-core-nebula` runtime config included `2a01:4f9:c01f:2::/64` as a dynamic Nebula firewall `local_cidr`, but `nebula-cert print` for `nixos-router-core-nebula` did not include that delegated GUA prefix in `unsafeNetworks`; packets entered local `nebula1` and never appeared on remote `hetz-router-nebula-core` `nebula1`. The renderer now emits `advertisedUnsafeNetworkSourceFiles` from modeled dynamic delegated firewall CIDRs, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-bootstrap-spec.sh` plus `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nix-file-loc.sh` pass. | reason=Dynamic delegated public-egress prefixes are now part of the explicit Nebula bootstrap/profile contract as source-file references; runtime secret resolution happens in orchestration without hardcoding the delegated prefix in renderer output or lab topology glue.
- state=solved | target=dynamic-delegated-nebula-peer-return-routes | evidence=2026-05-20 after the public runtime ingress fix, the full loop reached Hetz Nebula handshake and Site-C DNS A/AAAA over Nebula, but hostile public IPv6 egress still failed. Live tcpdump showed hostile GUA echo requests entered `hetz-router-nebula-core` on `nebula1` and left `upstream`; Cloudflare replies entered `upstream`, then the Hetz core routed the delegated `2a01:4f9:c01f:2::/64` destination back out `upstream` table 2002 instead of `nebula1`. The renderer now derives peer `dynamicUnsafeRoutes` from explicit source-file delegated runtime CIDRs, materializes them into the Nebula runtime config before service start, and validates the s-router three-site Hetz route with `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-nebula-dynamic-delegated-return-route.sh`; full renderer suite passed with `NETWORK_REPO_DIRECT_TEST_OK=1 ./tests/test.sh`. | reason=Peer return routes for dynamic delegated public prefixes are explicit CPM/provider output. The Nebula renderer may resolve source files at runtime and write Nebula `tun.unsafe_routes`, but NixOS host glue must not invent overlay return paths.
